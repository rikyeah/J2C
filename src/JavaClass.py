
class JavaClass:

    def __init__(self, package, imports, name, fields, methods):
        self.package = package
        self.imports = imports
        self.name = name
        self.fields = fields
        self.methods = methods
